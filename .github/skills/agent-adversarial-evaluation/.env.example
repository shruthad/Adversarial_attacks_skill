# Copy this file to .env and add keys only for approved non-production evaluation.
# Do not commit .env with real secrets.

# Used by LLM attack generation when runtime_config.yaml generation.mode is llm or hybrid.
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Optional: use separate keys/models by changing api_key_env in runtime_config.yaml.
# ATTACK_GENERATOR_API_KEY=
# LLM_JUDGE_API_KEY=
